%h1= @job.title
%p= @job.description
%p= @job.company

= link_to("Home", root_url)
= link_to("Edit", edit_job_url(@job))
= link_to("Delete", job_url(@job), method: :delete, data: { confirm: "Are you sure?" })
